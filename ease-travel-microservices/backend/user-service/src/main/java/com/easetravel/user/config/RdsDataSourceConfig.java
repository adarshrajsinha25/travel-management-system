package com.easetravel.user.config;

import lombok.extern.slf4j.Slf4j;
import org.springframework.context.annotation.Configuration;

/**
 * DataSource is now auto-configured by Spring Boot from application.yml
 * (spring.datasource.*). This class is kept as a placeholder.
 */
@Slf4j
@Configuration
public class RdsDataSourceConfig {
    // DataSource bean removed — Spring Boot auto-configures from application.yml
}
