server:
  port: 8085

spring:
  application:
    name: notification-service
  config:
    import: aws-secretsmanager:${RDS_SECRET_ARN:arn:aws:secretsmanager:us-east-1:959713282924:secret:rds!db-ac3131f7-1993-4580-8828-1622ea790e84-ZF7csy}
  datasource:
    url: jdbc:mysql://${host}:${port}/${DB_NAME:easetravel_notifications}?useSSL=true&requireSSL=true&serverTimezone=UTC&createDatabaseIfNotExist=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: ${username}
    password: ${password}
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: false
  cloud:
    aws:
      region:
        static: ${AWS_REGION:us-east-1}
  # Mail config — replace with real SMTP or use MailHog for local dev
  mail:
    host: localhost
    port: 1025
    username:
    password:
    properties:
      mail:
        smtp:
          auth: false
          starttls:
            enable: false

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    fetch-registry: true
    register-with-eureka: true
  instance:
    prefer-ip-address: true

management:
  endpoints:
    web:
      exposure:
        include: health,info

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html

logging:
  level:
    com.easetravel.notification: DEBUG

